# ????
server:
  port: 8080

# ?????
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/my-blog?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true
    username: root
    password: 261219Hyc
    driver-class-name: com.mysql.cj.jdbc.Driver

# MyBatis-Plus ??
mybatis-plus:
  configuration:
    # ???????????? user_id ????? Java ?? userId
    map-underscore-to-camel-case: true
    # ??SQL??????????????
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      # ??????
      id-type: auto

